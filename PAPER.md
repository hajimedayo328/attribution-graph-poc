# LLMの思考回路をグラフ理論で読む — 完全無料パイプラインによる機械論的解釈PoC

**個人研究ノート（2026年6月10〜22日）** / リポジトリ: https://github.com/hajimedayo328/attribution-graph-poc
公開ページ: https://hajimedayo328.github.io/attribution-graph-poc/

> 注: 本稿は探索的個人PoCの自分用整理であり、確立された知見の主張を目的としない。全数値は対応スクリプトの再実行で再現確認済み（2026-06-22）。詳細ログは `RESULTS.md`。

---

## Abstract

LLMの内部計算を可視化する attribution graph 計156枚（gemma-2-2b 144 + Qwen3-0.6B 12）と、線形プローブ・logit lens・MLPアブレーションを用いて、3モデル（gemma-2-2b / Qwen3-0.6B / Qwen3-1.7B）でLLMの「思考の形」を測った。完全無料のパイプラインで、(1) グラフ中心性による重要ノード再現、(2) 回路の言い回し支配と内容局在、(3) 「知ってるのに言えない」読み出し失敗の機構、(4) countingエラーの観察→分解→介入による犯人特定、を得た。同時に、当初の結論のいくつかを自分で反証した — アブレーションによる改善は「局所的」ではなく**トレードオフ**であり、数値知識の「2系統」は二分でなく**グラデーション**だった。負の結果（幻覚の要約統計シグネチャは作れない、reversal curse不発）も明示する。

---

## Introduction

機械論的解釈可能性（mechanistic interpretability）は、LLMの内部計算を「読める」回路として取り出す試みである。本PoCはそこに、グラフ理論で訓練された自分の視点を持ち込めるか、という個人的な問いから始まった。Anthropicが公開した attribution graph（モデルの推論を特徴ノードと因果エッジの有向グラフとして可視化する手法）を見て、これはグラフだ、ならNetworkXの統計や中心性指標で「思考の形」を測れるのではないか、と考えたのが出発点である。

attribution graph は、元のモデルそのものではなく、MLPを解釈可能な特徴（transcoder/置換モデル）に置き換えた近似上で計算される。各ノードは特定の層・位置の特徴、エッジはノード間の因果的寄与を表す。重要なのは、これが原モデルの完全な写像ではない点だ。誤差項（本PoCの計測で誤差ノード比率 0.08〜0.18）が示す通り、我々が見ているのは「近似された思考」であり、特に counting のような苦手タスクでは近似誤差が大きい。この限界は結論の随所に効いてくる。

本PoCの貢献は4点ある。第一に、**完全無料のパイプライン**を組んだ。Neuronpediaの無料API（匿名生成、1日約28枚の制限あり）と、ローカルGPU（RTX 5070 Ti）上の circuit-tracer を併用し、計156枚の attribution graph を生成した。第二に、**3モデル比較**（gemma-2-2b / Qwen3-1.7B / Qwen3-0.6B）で発見の普遍性とモデル依存性を切り分けた。第三に、**探索的に仮説を立てて自分で反証する方法論**を一貫して採った。「同じ思考は同じ回路を使う」は「同じテンプレートが同じ回路」へ修正され、「LLMの失敗は読み出しの失敗」という統一テーマは別タスクで境界が引かれ、「アブレーションによる局所的改善」は難タスクでトレードオフへと覆った。第四に、これら**負の結果と訂正を隠さず明示する**。幻覚誘発の3連敗、reversal curse の不発、グラフ要約統計による幻覚/架空検出の頭打ち（AUC 0.73〜0.75）は、いずれも失敗として記録した。

本PoCは単一シード・小型モデル中心・小標本という限界を抱え、確立された知見の提示を目的としない。価値は、検証可能な形で仮説を立て、自分の手で反証していく健全なサイクルそのものにある。

---

## Methods

本研究は外部GPU予算ゼロ・無料枠のみで遂行した個人PoCである。

**データ生成.** attribution graph は計156枚を用いた — gemma-2-2b 144枚（Neuronpedia の無料生成API、transcoder: `gemmascope-transcoder-16k`）と、Qwen3-0.6B 12枚（circuit-tracer をローカルRTX 5070 Tiで実行）。加えて Qwen3-1.7B を5枚部分生成した（transcoder 37.6GB をRAM 32GBへロードしてフリーズ事故を起こし中断。以降はVRAM側ロード＋ウォッチドッグで対処）。匿名APIは1日約28枚で429（レート制限）に達するため複数日に分けて生成した。全グラフは `manifest.csv`（gemma 144行）から再取得でき、生成スクリプトと共に再現性を確保した。なお0.6Bの transcoder は1層163,840特徴とgemma（16k）の約10倍で、「特徴の粒度」がモデル間比較の本質的交絡となる点は限界として明示する。

**グラフ統計（NetworkX）.** 各グラフに対し、logit起点の個人化PageRank、重み付き次数（出/入/総）、特徴量集合のJaccard類似度、participation ratio（実効寄与本数 (Σw)²/Σw²）、レイヤー重心・密度・modularity・degree Gini・error_share 等を算出した。`influence` フィールドは累積枝刈りスコアで小さいほど重要なため符号反転して扱った。

**回路解析（TransformerLens）.** 線形プローブはリッジ回帰で residual stream から目標量（文字数・年）を予測した。初出は8:2分割×5シードだが、年想起の R² が分割で不安定だったため40分割平均で再推定した（実験S-2）。logit lens で中間表現をモデル自身の出力層により復号し、正解トークンの浮上・消失を追跡。MLPアブレーション（介入）は `run_with_hooks` で特定層のMLP出力をスケール（例: L24-27×0.5）し正答率変化を測定。direct logit attribution で層・部品別の寄与も分解した。

**統計検定.** 群間比較は Mann-Whitney U 検定に Benjamini-Hochberg（BH）補正を適用し q<0.05 を有意とした。小標本（n=12〜60）では検出力不足を明記し、BH補正後に有意指標がゼロになった結果も負の結果として保持した。

**プロンプト設計.** few-shot形式と、Neuronpedia steering API のデフォルト logprobs によるスクリーニング（グラフ生成クォータを消費しない）を併用し、「数字を答える出力形式」等をグラフ生成前に確定させた。これは幻覚誘発の3連敗を経て確立した手順である。

---

## Results

仮説→検証→自己反証のサイクルで得た結果をテーマ別に示す。

### 重要ノードの再現はランキングの「頭」でのみ大域構造が効く
logit起点の個人化PageRankで circuit-tracer 公式の因果的重要度を再現できるか検証した（n=60）。順位相関は自明な重み付き出次数（Spearman ρ=0.691）とPageRank（0.686）がほぼ互角で、大半のノードの重要度は局所情報で決まる。一方、最重要top10の特定ではPageRankが圧勝（6.67/10 vs 出次数3.84/10）。「PageRankで重要ノードが分かる」と主張するなら ρ ではなく top-k precision で言うべきで、ρでの主張は半分が自明である。

### 回路は言い回しに支配され、内容は局在する（3モデル普遍）
特徴量集合のJaccard類似度で回路の共有度を測ると、同じ言い回し・違う国=0.668、同じ国・違う言い回し=0.284で、回路の同一性は表面形式が支配する。ただし国トークン位置だけに絞ると0.404へ逆転し、1-NN多数派も「同じ国」側になる（内容回路は実在するが空間的に局在）。この言い回し支配と内容局在は gemma-2-2b・Qwen3-1.7B・0.6B の3モデルで一貫して再現した。

### 「知ってるのに言えない」=読み出しの失敗（2レジーム、Qwenで数値再現）
国トークン位置から出力への情報流を測ると、logit直結シェアは答えた群0.274 / 答えない群0.075で12グラフ完全分離。失敗には2レジームがあり、(1) 候補が不在（回答モード不起動）、(2) 首都が2位まで来て汎用語 " a" に競り負ける（読み出し競争で敗北）に分かれる。Qwen3-0.6Bで答えた群0.266 / 答えない群0.059と数値までほぼ一致し、本プロジェクト最強の普遍性結果となった。

### countingエラーの追跡 — 読み出しの失敗が犯人MLPまで
countingエラーは離散的な回路選択ミスとして可視化されず（誤答特異的な特徴0個・実験O）、トークン化と計数容量の二段構えに分解できた（文字バラしで誤答13語中8語が正答化・実験P）。線形プローブは文字数をR²=0.94で読めるのに出力正答率は0.45（実験R）。logit lensでは最終層で誤答した28件（誤答の61%）で、正解が中間層（L23付近）でいったん1位（top-1）まで浮上した後、最終2層で2位へ転落して誤答に競り負けていた（実験T）。犯人は終盤MLP（主犯L25、寄与29%、手口は +1 オーバーシュート）で、L24-27 MLPを0.5倍にすると正答率0.45→0.57へ改善した（実験U）。観察（logit lens）→分解（寄与）→介入（アブレーション）の三段で因果まで通した。

### 介入はトレードオフ（局所的改善ではない — 自己訂正）
実験Uの「副作用なし」は、当初の対照タスクが満点（天井効果）で副作用を見逃していた。難タスクで再測定すると、L24-27×0.5 は counting +0.155 の代わりに3桁加算 −0.20・首都想起 −0.16 を払うトレードオフだった（実験V-3）。終盤MLPは複数タスクの「精度仕事」を担う共有資源である。介入強度を下げた L24-27×0.7 が唯一の純益プラス（+0.021）のPareto点で、派手な「+12pt」は副作用込みで評価すべきという結論に訂正した（実験V-4）。

### 数値知識の2系統はグラデーション（自己訂正）
「失敗は読み出しの失敗」仮説を年の事実想起で検証すると即反証された。年は線形プローブで R²=0.60±0.16 と分割乱数で不安定（n=60で検出力不足）であり、「読めない」ではなく「counting=0.94ほどではないが中程度に読める」が正確（実験S/S-2）。すなわち「線形の数直線（文字数）」と「カテゴリカルな検索（年）」の2系統は二分でなくグラデーションである。この区別の主柱は logit lens（counting 61%が浮上→消失 / 年は0%＝浮上したら消えない）で、この質的差は頑健に維持される。線形プローブのR²差は副次証拠に格下げした。

### 幻覚の要約統計シグネチャは作れない（負の結果）
グラフ要約統計から架空質問・counting誤答を検出できるか検定した。架空質問の構造分類は AUC 0.75 で頭打ち（実験D）。counting誤答は n=40 で density 等の方向は一貫（誤答=高密度・高エントロピー）だが、BH補正後に有意な指標はゼロ。n=12で出ていた density AUC 0.829 は n=40 で 0.729 へ後退し、小標本の幸運（平均への回帰）だった。要約統計では幻覚検出器は作れないことが確定的で、残る道はノードレベル・位置レベルの情報である。

---

## Limitations

誠実さのため、結論を弱める事実を列挙する。

- **手法上の近似**: 分析対象の attribution graph は元モデルそのものではなく replacement model（transcoder置換）の近似である。誤差ノード比率 error_share は首都系で 8〜10%、counting系では約18%に達し、特に counting では「観ている構造差が思考の差ではなく近似誤差の癖」である危険が高い。circuit-tracer のグラフは skip 接続が多く、経路ホップ数は無意味だった（実験B）。
- **標本と一般化**: 各群 n=10〜60 と小さく、モデルは gemma-2-2b / Qwen3-0.6B / 1.7B の最大3種、単一シード・単一テンプレート・greedy（argmax）のみ。探索フェーズが幅優先だったため invariant 同士が相関しており、独立な発見数は見かけより少ない。universality の主張も実質2タスク・小型モデルに限られる。
- **統計的検出力**: 幻覚シグネチャは n=12 で出た density AUC 0.829 が n=40 で 0.729 に後退（平均への回帰）。線形プローブの年想起 R² も分割乱数で 0.5〜0.7 に振れ、安定推定で 0.60±0.16（n=60 が d_model=1024 に対し過小）。対照タスクは算術が満点で天井効果が強く、中域の難度を作れなかった。
- **介入の限界**: MLPアブレーションの副作用測定は当初「局所的改善」と結論したが、対照が天井で見逃していた。難タスクで再測定すると L24-27×0.5 は counting +0.155 と引き換えに3桁加算 −0.20・首都 −0.16 のトレードオフであり、Pareto 点は弱い介入（L24-27×0.7）だった。副作用測定自体も baseline がほぼ満点の領域での観測である。steering は全トークン位置に一様な鈍器で、位置特異的な計算とは不整合。
- **解釈の不確実性**: auto-interp による特徴説明は AI 生成で個々の正確性は未保証であり、傾向としてのみ読むべきである。

---

## 付記: 実験対応表

| 章 | 主な実験 | スクリプト |
|---|---|---|
| 重要ノード再現 | 結果1 | `analyze_batch.py` |
| 言い回し支配・内容局在 | F, G | `followup4.py` |
| 読み出し失敗の機構 | I, I-2 | `followup6.py`, `qwen/qwen06_transport.py` |
| 支持基盤の広さ(PR) | K | `pr_gemma.py` |
| counting追跡 | O, P, R, T, U | `n_nodelevel.py`, `qwen/probe_experiment.py`, `qwen/logitlens_experiment.py`, `qwen/culprit_experiment.py` |
| 介入トレードオフ | V, V-3, V-4 | `qwen/sideeffect_experiment.py`, `qwen/sideeffect_v3.py`, `qwen/sideeffect_v4.py` |
| 2系統グラデーション | S, S-2 | `qwen/probe_years.py`, `qwen/probe_years_gen.py` |
| 幻覚の負の結果 | D, M-2 | `followup2.py`, `analyze_batch.py` |
