# 圏論スケッチ: 「自然同型なき不変量の一致」 (遊び / 賈先生持ち込み用素材, 2026-07-03)

> **ラベル: 遊び・形式化スケッチ**。計算的裏付けは既存の実測値のみで、圏論部分は「言い換え」であって新しい定理ではない。
> 飾り化の一線: このスケッチが価値を持つのは「先行の経験的発見を正確に言い直せている」限りにおいて。

## 設定

- **C_series**: 対象 = 古典10系列の漢字39字。射 = 「同じ音符系列に属する」という同値関係が生成する亜群 (groupoid)。
- **Met**: (擬)距離空間の圏。
- 各モデル m に対し **F_m : Ob(C_series) → Met**: 漢字 ↦ 中間層埋め込み (次元z-score正規化後の点)。

## 実測が言っていることの圏論的言い換え

**(1) F は系列構造を距離レベルで忘れる。**
系列内距離 1.401 ≒ 系列間距離 1.433 (比 1.023)。つまり C_series の射 (同系列性) は F の像でほとんど「縮まない」— F は亜群構造を保つ計量関手では**ない**。にもかかわらず H₁ という大域不変量が立つ (z=+2.7, 清潔null)。**局所的に忘れられた構造が、大域不変量としてだけ現れる**。

**(2) モデル間に自然変換は無い。しかし不変量は一致する。**
恒等 (漢字の同一性) の上での自然同型 η : F_LLM-jp ⇒ F_Qwen3 が存在するなら、計量形状は一致するはず。実測: Gromov-Wasserstein は null (z=-0.4)、個別辺の Jaccard は 0.055 — **自然同型は経験的に棄却**。
一方、位相不変量関手 I : Met → ℝ³ (H₁ ratio, 種, 増幅) を合成すると:
I∘F_LLM-jp ≅ I∘F_Qwen3  —  (1.63, 1.22, +0.41) ≒ (1.61, 1.25, +0.35)
**「表現としては自然同型でないが、不変量の商に落とすと同型」** — ホモロジーが同じで同相でない空間のペアの、LLM版。

**(3) 拡張に対する非関手性 (P6の言い換え)。**
包含 C_series ↪ C_series+22 に沿って構造は保たれなかった (拡張で H₁ 消失、追加系列は null)。系列→位相の割り当ては部分圏の包含に関して**関手的でない** — 構造は「対象の追加」という余極限的操作で壊れる。スワップテスト (97-99.5パーセンタイル) は、この特定の部分圏だけが不変量を担うことを言っている。

**(4) 機能との随伴の不在 (二重乖離改め単方向乖離の言い換え)。**
行動能力の関手 B (読み/韻の強制選択スコア) と I∘F の間に相関的な対応は無い (位相ゼロで B 満点)。**表現の不変量は行動の不変量を決定しない** — 内部構造圏から行動圏への「意味を運ぶ関手」がここには立たない。これは賈先生・Floridi の「LLMは記号接地を回避する」の具体的一断面として読める: 言語学的構造 (音符) は内部に不変量として実在するが、それは接地 (機能・行動) と関手的に繋がっていない。

## 賈先生に投げる問い (これがスケッチの目的)

1. 「自然同型なき不変量の一致」を正確に述べる枠組みはあるか — 表現の圏の局所化 / 不変量による商圏で、F_m たちが同一対象に落ちる、という定式化は正当か。
2. (3) の「部分圏にのみ担われる不変量」は、圏論的に面白い現象か、それとも単に「性質が部分対象に局在する」以上の内容が無いか。
3. この現象群に enriched category (Lawvere計量圏) の言葉を使う価値はあるか — F を Met-enriched functor と見た時、(1) は enrichment の退化として言えるか。

## 使った実測値の出典
RESULTS_SUMMARY.md §1 (ratio/種/増幅), §4 (P6/スワップ), §6 (行動), 追記14 (GW)。
